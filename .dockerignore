# 只排除与构建无关的上下文。
node_modules
build
.svelte-kit
# Rust 构建产物与本机订阅测试数据（数 GB / 数百 KB，严禁进构建上下文）
static-server/target
static-server/.data
.git
.github
.vscode
.zcode
tests
# worker 是独立的 Cloudflare Worker 部署单元（OAuth，可选自建），容器构建不依赖
worker
.env
.env.*
!.env.example
.dev.vars
.DS_Store
